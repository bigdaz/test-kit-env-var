To check if TestKit is passing environment variables correctly:
```
cd test-kit-runner
./gradlew test
```

This is what is would look like if the env var isn't being passed: https://ge.solutions-team.gradle.com/s/vmjwxo7dv3kle/tests/overview
